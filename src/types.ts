export interface Product {
  id: string;
  name: string;
  category: string;
  headline: string;
  description: string;
  price: string;
  image: string;
  accent: string;
  features: string[];
}

export interface StoryLine {
  title: string;
  text: string;
}
